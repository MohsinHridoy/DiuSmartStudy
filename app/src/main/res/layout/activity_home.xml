<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".home.HomeActivity"
    android:orientation="vertical"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Book Category are Here"
        android:textColor="@color/colorPrimary"
        android:textSize="30dp"

        android:layout_marginTop="7dp"
        android:padding="@dimen/dp_20"
        android:fontFamily="@font/sacramentoregular"



        />

    <androidx.recyclerview.widget.RecyclerView
        android:layout_marginTop="20dp"
        android:id="@+id/rvHeader"
        android:layout_width="match_parent"
        android:layout_height="170dp"
        tools:listitem="@layout/vh_header_bookitem"

        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/open_book"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
         android:layout_gravity="center"
        android:text="Upload Books"
        android:fontFamily="@font/poppinsbold"


        android:textColor="@color/colorPrimary"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:background="@drawable/white_round_back"
        android:elevation="12dp"
        android:layout_margin="@dimen/dp_10"
        android:padding="@dimen/dp_10"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvBooks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/rv_book_item"


        />

</LinearLayout>